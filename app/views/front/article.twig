
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Articles</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 font-sans">

    <!-- Navbar -->
    <nav class="bg-indigo-600 p-4">
        <div class="container mx-auto flex justify-between items-center">
            <a href="{{ path('home') }}" class="text-white text-xl font-bold">MonSite</a>
            <div>
                <a href="{{ path('login') }}" class="text-white px-4 py-2 hover:bg-indigo-700 rounded">Se connecter</a>
                <a href="{{ path('signup') }}" class="text-white px-4 py-2 hover:bg-indigo-700 rounded">S'inscrire</a>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="bg-indigo-600 text-white py-16">
        <div class="container mx-auto text-center">
            <h1 class="text-4xl font-bold mb-4">Tous nos Articles</h1>
            <p class="text-lg mb-8">Découvrez nos derniers articles sur des sujets variés.</p>
        </div>
    </section>

    <!-- Articles Section -->
    <section class="py-16">
        <div class="container mx-auto px-4">
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-12">
              
                    <div class="bg-white rounded-lg shadow-lg overflow-hidden">
                        <img src="../public/assets/article.png" alt="{{ article.title }}" class="w-full h-48 object-cover">
                        <div class="p-6">
                            <h2 class="text-2xl font-semibold text-gray-800 mb-2">{{ article.title }}</h2>
                            <p class="text-gray-600 text-base mb-4">{{ article.excerpt }}</p>
                            <a href="{{ path('article_detail', {'id': article.id}) }}" class="text-indigo-600 hover:text-indigo-700 font-semibold">Lire l'article</a>
                        </div>
                    </div>
               
                    <p class="text-center text-gray-600 col-span-3">Aucun article trouvé.</p>
              
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white py-6">
        <div class="container mx-auto text-center">
            <p>&copy; 2025 MonSite. Tous droits réservés.</p>
        </div>
    </footer>

</body>
</html>
